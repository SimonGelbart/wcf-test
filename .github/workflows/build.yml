name: Build

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Restore
        run: dotnet restore CoreWcfJwtSoap/CoreWcfJwtSoap.csproj
      - name: Restore integration runner
        run: dotnet restore SoapApi.IntegrationTests/SoapApi.IntegrationTests.csproj
      - name: Build
        run: dotnet build CoreWcfJwtSoap/CoreWcfJwtSoap.csproj --no-restore --configuration Release
      - name: Build integration runner
        run: dotnet build SoapApi.IntegrationTests/SoapApi.IntegrationTests.csproj --no-restore --configuration Release
